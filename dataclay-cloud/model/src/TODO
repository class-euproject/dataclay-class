1. Build the dockers with the new model -> Events federated and appended manually to history of objects

2. When object unfederated (when_unfederated) -> remove it from DKB, as it is still present there so far. Anyways, dataClay team is currently working on a bug they have with aliases.
